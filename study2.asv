%MATLAB矩阵
x = 20 + (50-20)*rand(5);

%关系运算 
%< <= >= == ~=
%C=A>B  A,B维度相同，同位置上，A的值若大于B，则返回值为1，否则为0
A = [1,2,3;1,2,3;1,2,3];
B = [2,2,3;2,2,3;2,2,3];
C = A < B;

%复杂度是一个很重要的概念，让复杂度变小

D = fix((90-1-+1)*rand(5)+10)%"去学这些语法""""""
E = rem(D,3) == 0%判断D中元素能否被3整除
%在MATLAB中，能用矩阵表达就用矩阵表达，可以使效率变高，复杂度